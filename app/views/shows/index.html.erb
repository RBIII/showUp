<h1>Shows</h1>

<ul>
  <% @shows.each do |show| %>
    <li>
      <%= link_to show.band.name, show_path(show) %>
      <%= link_to show.venue.name, venue_path(show.venue) %>
      <%= show.date %>
      <%= show.time %>
    </li>
  <% end %>
</ul>
